<!DOCTYPE html>
  <html lang="en">
      <head>
          <meta charset="UTF-8">
          <!--  costum css  -->
          <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css" integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">
          //<title>Node Server</title>
          title= title
          <link rel="stylesheet" type="text/css" href="css/style.css">
      </head>
      <body>
          <div class="jumbotron">
              include navigation.pug
              <h1>Klassentabelle</h1>
          </div>
          <div class="container">
              <div class="row">
                  <div class="col-4">
                      <h3>In Datenbank schreiben:</h3>
                      <div class="input-group mb-3">
                          <form method="post">
                              <div class="form-group">
                                  <small id="vorname" class="form-text text-muted">Wir teilen Ihre Daten mit niemandem!</small>
                                    <select name="klassenlehrer_id" class="form-select form-select-lg" aria-label="Default select">
                                    option(selected)= "Klassenlehrer auswählen..."
                                     if klassenlehrer
                                       each val in klassenlehrer
                                        option(value=val.id)= val.l_nachname + ", " + val.l_vorname
                                    </select>
                              </div>
                              <div class="form-group">
                                    <select name="klassensprecher_id" class="form-select form-select-lg" aria-label="Default select">
                                    option(selected)= "Klassensprecher auswählen..."
                                     if klassenlehrer
                                       each val in klassensprecher
                                         option(value=val.id)= val.s_nachname + ", " + val.s_vorname
                                    </select>
                              </div>
                              <div class="form-group">
                                  <input name="bezeichner" aria-label="klassenname" placeholder="Klassenname" type="text" class="form-control" id="inputKlassenname">
                              </div>
                              <div class="form-group">
                                  <input name="jahrgang" aria-label="jahrgang" placeholder="Jahrgang" type="text" class="form-control" id="inputJahrgang">
                              </div>
                              <button type="submit" class="btn btn-primary">Nutzer erstellen</button>
                          </form>
                      </div>
                  </div>
                  <div class="col-8">
                      <h3>Aus Datenbank lesen: Tabelle Schulklasse</h3>
                      <table class="table table-hover">
                          <thead>
                               <tr>
                                   th
                                       form(method="get")
                                           button(class="btn btn-link btm-sm" type="supmit" name="order_by" value="id" )='#'
                                   th
                                       form(method="get")
                                           button(class="btn btn-link btm-sm" type="supmit" name="order_by" value="bezeichner" )="Klassenname"
                                   th
                                       form(method="get")
                                           button(class="btn btn-link btm-sm" type="supmit" name="order_by" value="jahrgang")="Jahrgang"
                                   th
                                       form(method="get")
                                           button(class="btn btn-link btm-sm" type="supmit" name="order_by" value="klassenlehrer_id")="Klassenlehrer"
                                   th
                                       form(method="get")
                                           button(class="btn btn-link btm-sm" type="supmit" name="order_by" value="klassensprecher_id")="Klassensprecher"
                                           <th scope="col"></th>
                               </tr>
                          </thead>
                          <tbody>
                          each val in schulklasse
                            tr
                              td(type=button class="btn-link")= val.id
                              td= val.bezeichner
                              td= val.jahrgang
                              td= val.l_nachname + ", "+ val.l_vorname
                              td= val.s_nachname + ", "+ val.l_nachname
                              td
                                  form(method="post")
                                      button(type='submit' method="post" class="btn btn-outline-danger" name="id" value=val.id)= "Löschen"
                          </tbody>
                      </table>
                  </div>
              </div>
          </div>
      </body>
  </html>
